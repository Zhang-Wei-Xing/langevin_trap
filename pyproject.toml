[project]
name = "langevin-trap-simulation"
version = "0.1.0"
description = "A Brownian particle simulation in a damped harmonic potential well."
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
]

[tool.setuptools]
py-modules = ["hello", "langevin_trap"]
